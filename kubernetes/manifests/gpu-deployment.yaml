apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama-gpu
spec:
  template:
    spec:
      containers:
      - name: ollama
        image: ollama/ollama:latest
        resources:
          limits:
            nvidia.com/gpu: 1
